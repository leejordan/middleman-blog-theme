---
title: Home page
---

<%= partial 'partials/intro', :locals => { :offset => true } %>

<section class="page-section">
    <div class="container">
        <h2><a href="/blog">My Blog</a></h2>
        <p class="lead">
            A collection of blog posts and case studies about UX and web development.
        </p>
    </div>
    <% page_articles.first(1).each_with_index do |article, index| %>
        <%= partial 'partials/feature', :locals => {
                :url => '/posts/' + article.slug,
                :img => '/images/posts/' + article.slug + '/cover.jpg',
                :title => article.title,
                :body => article.date.strftime('%b %e %Y')
            } %>
    <% end %>
    <div class="container">
        <p class="lead"><a href="/blog">View all blog posts</a></p>
    </div>
</section>

<section class="page-section">
    <div class="container">
        <h2><a href="/portfolio">My Web Portfolio</a></h2>
        <p class="lead">
            A curated list of some of the products I have been involved with.
        </p>
    </div>
    <% data.portfolio.all.first(1).each do |portfolio| %>
        <%= partial 'partials/feature', :locals => {
                :url => portfolio.url,
                :img => '/images/portfolio/' + portfolio.slug + '.jpg',
                :title => portfolio.name,
                :body => portfolio.description
            } %>
    <% end %>
    <div class="container">
        <p class="lead"><a href="/portfolio">View all portfolio entries</a></p>
    </div>
</section>

<section class="page-section">
    <div class="container">
        <h2><a href="/experiments">My art experiments</a></h2>
        <p class="lead">
            Experiments with CSS, layout, and web design in general. Yes, it <strong>IS</strong> art.
        </p>
    </div>
    <% data.experiments.all.first(1).each do |experiment| %>
        <%= partial 'partials/feature', :locals => {
                :url => '/experiments/' + experiment.slug,
                :img => '/images/experiments/' + experiment.slug + '/cover.jpg',
                :title => experiment.name,
                :body => experiment.description
            } %>
    <% end %>
    <div class="container">
        <p class="lead"><a href="/experiments">View all art experiments</a></p>
    </div>
</section>
